# Baekjoon-Algorithm
##10250
    1 -> 01
   str(x).zfill(2) (문자열에 사용함)

아스키 코드 -> ord("문자")

##B2 - 2675
    abc 같은 문자열 리스트 만들기 -> 그냥 lst(abc) 하면 됨
    for char in lst: => lst 안에 char 문자들 반복 함, 인덱싱 안해도 됨.


##B2 - 8958
    0010101 -> list(input()) 하면 리스트에 \n 도 들어감. 
    list(input().strip())해주면 사라짐.

##B1 - 2775
    #처음맛보는 DP, 시간초과의 늪.. 재귀가 모든 시간초과를 해결해주지않는다
    "다이나믹 프로그래밍 ->  DP가 성립하려면 다음 두 가지 조건이 필요한데,
    부분 문제가 겹친다 (Overlapping Subproblems): 2층 3호를 구하려면 1층 1, 2, 3호가 필요하고, 2층 2호를 구하려면 또 1층 1, 2호가 필요. 즉, 똑같은 계산이 계속 반복됩니다.작은 문제의 합이 큰 문제의 정답이 된다